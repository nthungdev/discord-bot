name: chat-bot-prod

services:
  server:
    container_name: server
    environment:
      NODE_ENV: production
      PORT: ${PORT:-3001}
    env_file:
      - ./.env.production
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
