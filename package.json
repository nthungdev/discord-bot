{
  "name": "discord-bot-root",
  "private": true,
  "scripts": {
    "dev:app": "pnpm --prefix app dev",
    "dev:web": "pnpm --prefix web dev",
    "build": "pnpm --filter chat-bot-app build && pnpm --filter discord-bot-web build",
    "typecheck": "pnpm --filter chat-bot-app typecheck && pnpm --filter discord-bot-web build",
    "test": "pnpm --filter chat-bot-app test",
    "lint": "pnpm --filter chat-bot-app lint",
    "format": "pnpm --filter chat-bot-app format"
  }
}
